puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes

puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodesputs 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes
puts 'insert a simple code'
code=gets
eval code
 
someCodes="puts 'whats your name?'
        name=gets
        puts name.upcase!()
"
eval someCodes

